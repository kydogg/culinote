//
//  Constants.swift
//  Culinote
//
//  Created by Kyle D. Baker on 10/13/24.
//

import UIKit

enum SFSymbols {
    static let breakfast = UIImage(systemName: "sunrise.fill")        // Breakfast
    static let lunch = UIImage(systemName: "fork.knife")              // Lunch
    static let dinner = UIImage(systemName: "fork.knife.circle.fill") // Dinner
    static let dessert = UIImage(systemName: "leaf.fill")             // Dessert (You can replace with "cupcake.fill" if you design it)
    static let snacks = UIImage(systemName: "takeoutbag.and.cup.and.straw.fill") // Snacks
    static let appetizers = UIImage(systemName: "smallcircle.filled.circle")     // Appetizers
    static let drinks = UIImage(systemName: "drop.fill")              // Drinks (Alternate: "wineglass.fill")
    static let salads = UIImage(systemName: "leaf.fill")              // Salads
    static let soups = UIImage(systemName: "flame.fill")              // Soups (Alternate: "drop")
    static let vegetarian = UIImage(systemName: "leaf.arrow.triangle.circlepath") // Vegetarian
    static let vegan = UIImage(systemName: "leaf.circle.fill")        // Vegan (Alternate: "carrot.fill" if custom)
    static let glutenFree = UIImage(systemName: "allergens")          // Gluten-Free (You can design a wheat-free icon as custom)
    static let ketoLowCarb = UIImage(systemName: "hare.fill")         // Keto/Low-Carb (Alternate: "circle.lefthalf.fill")
    static let pasta = UIImage(systemName: "fork.knife.circle")       // Pasta
    static let bakedGoods = UIImage(systemName: "baguette.fill")      // Baked Goods (Alternate: "flame.fill")
    static let slowCooker = UIImage(systemName: "timer")              // Slow Cooker (Alternate: "hourglass")
    static let grillingBBQ = UIImage(systemName: "flame")             // Grilling/BBQ (Alternate: "grill.fill")
    static let seafood = UIImage(systemName: "fish.fill")             // Seafood (Alternate: "tortoise.fill")
    static let healthy = UIImage(systemName: "heart.circle.fill")     // Healthy (Alternate: "leaf.fill")
    static let comfortFood = UIImage(systemName: "house.fill")        // Comfort Food (Alternate: "cloud.fill")
    static let international = UIImage(systemName: "globe")           // International (Alternate: "flag.fill")
}
